{
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && functions-framework --target=handler",
    "prestart": "npm run build"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^3.4.5"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.13.4",
    "typescript": "^5.7.3"
  }
}
